services:
  # MySQL service - commented out to use external Cloud SQL
  # mysql:
  #   image: mysql:8.4
  #   environment:
  #     MYSQL_DATABASE: learning_quest
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #   ports:
  #     - "3307:3306"
  #   healthcheck:
  #     test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p'$${MYSQL_ROOT_PASSWORD}' >/dev/null 2>&1"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 30
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #     - ./schema.sql:/docker-entrypoint-initdb.d/schema.sql
  #
  backend:
    build: ./backend
    restart: on-failure
    environment:
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      JWT_SECRET: ${JWT_SECRET}
      EMAIL_USER: ${EMAIL_USER}
      EMAIL_APP_PASSWORD : ${EMAIL_APP_PASSWORD}
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    environment:
      VITE_PROXY_TARGET: http://backend:3000
    ports:
      - "5173:5173"
    depends_on:
      - backend